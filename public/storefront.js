const search=document.querySelector('#catalog-search'),category=document.querySelector('#catalog-category');
if(search&&category){const cards=[...document.querySelectorAll('.product-card')];function filter(){const q=search.value.trim().toLocaleLowerCase('th-TH');let count=0;for(const card of cards){const show=card.dataset.name.toLocaleLowerCase('th-TH').includes(q)&&(!category.value||card.dataset.category===category.value);card.hidden=!show;if(show)count++;}document.querySelector('#catalog-results').textContent=count+' รายการ';document.querySelector('#catalog-empty').hidden=count>0||cards.length===0;}search.addEventListener('input',filter);category.addEventListener('change',filter);search.closest('form').addEventListener('submit',ev=>ev.preventDefault());}
